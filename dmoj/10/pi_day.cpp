#include <bits/stdc++.h>

using namespace std;

  int main() {
    int n, k;
    // n = # of pie pieces (1 <= n <= 250)
    // k = # of people (people in line)
    cin >> n >> k;

    if(n == k || k == 1) return 1;

      

    return 0;
    // find greatest gate # <= port #
  }
